"""SimClock, scenario schema, and synthetic curve generators.

Reserved, empty package. Populated in M2 (simulation engine &
synthetic data generator), which everything downstream (agents,
orchestrator) is tested against before any real sensor integration.
"""
