"""Root package for the Industrial Safety Intelligence Platform backend.

Exists to make ``src`` importable as a namespace root for the four
architectural layers (api, services, domain, infra) plus config. Every
other package under ``src`` is a future milestone's home; this file has
no logic and never will.
"""
