"""WebSocket connection manager and Redis subscriber (reserved for M7).

Exists now, empty, so the api/ layer's directory structure is fixed
from M0 onward and later milestones never need to restructure it.
No code lives here until M7 (WebSocket streaming layer).
"""
